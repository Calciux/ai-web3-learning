# 打卡 2026-06-08

## 今天做了什么

- hackrepo git commit + push（14 文件，含 ERC8183Escrow.sol + MockERC20 + 全文档 + Agent 脚本骨架）
- commit message: `feat: initial ERC-8183 escrow contract + Foundry setup + agent scripts`
- 修复 .gitignore（补充 cache/ out/ lib/）和 README 恢复
- 解决 WSL 下 SSH push 问题：GitHub SSH-over-HTTPS（443 端口）绕过代理封锁
- 配置 ~/.ssh/config：`Host github.com → HostName ssh.github.com Port 443 + ProxyCommand nc`
- 设计四 Agent 单元测试流水线：Plan → Implement → Review → Executor
- 编写 4 份 Agent 提示词（scratch/prompt/unit-test/）
- Plan Agent 产出 104 用例的单元测试清单（test/unit/unit-test-checklist.md）
- Implement Agent 产出 15 个 .t.sol + 6 个 Mock 合约
- **解决 forge 无法运行**：WSL Ubuntu 20.04 GLIBC 2.31 太老 → patchelf + 独立 GLIBC 2.35（~/.local/glibc-2.35/）绕过
- **解决 Natspec 编译 crash**：solc 0.8.21 → 0.8.25（foundry.toml）
- forge-std v1.16.1 → v1.9.5 降级（solc 0.8.21 兼容性问题已在 0.8.25 下消失）
- 104 单元测试全部通过（101/104 first run，3 个测试代码 bug 修复后全绿）
- commit + push: `feat: 104 unit tests + contract fixes + solc 0.8.25 upgrade`（29 文件，+3252/-275 行）
- **集成测试完整清单**：Plan Agent 产出 IT-003~IT-022（20 用例，覆盖 Reject/Expire/Hook/Reentry/MultiJob/Edge）
- **目录重组**：test/integration/ → happypath/（IT-001/IT-002）+ full/（IT-003~IT-022）
- **四 Agent 集成测试流水线**：Plan → Implement → Review → Executor prompt 全部写毕
- Implement Agent 产出 5/6 测试文件（CrossReentry.t.sol 缺失，IT-015/IT-016 未实现）
- **Sepolia 真链测试**：
  - 部署 2 个新 MockERC20（USDT: `0x8c7D...0BEb`, ETH mock: `0x9402...FE99`）
  - 编写 FundTransferHook（3 次迭代，V1 staticcall 解码有 bug）
  - 最终 SimpleSwapHook（`0x3e60...ecA4`）成功跑通 Job 4
  - **Job 4 全链路**：createJob → setBudget(Hook optParams) → fund → submit → complete
  - 证据文件：scratch/job4-evidence.md

## 学到的重点

- GitHub 提供 ssh.github.com:443 作为 SSH-over-HTTPS 端点，绕过企业/ISP 对 22 端口的封锁
- nc -X connect -x <proxy> %h %p 可让 SSH 走 SOCKS/HTTP 代理
- git add 支持按目录批量添加：`git add contracts/ docs/ scripts/ test/`
- **patchelf** 可修改 ELF 二进制的动态链接器（interpreter）和 rpath，不替换系统 GLIBC 就能跑新版二进制
- solc 0.8.21 Natspec.cpp(85) 有编译器断言 crash bug，0.8.25 已修复
- Forge nightly build 可通过 GitHub Releases 直接下载（`foundry_nightly_linux_amd64.tar.gz`）
- forge 下载 solc 二进制需走 HTTPS_PROXY（`export HTTPS_PROXY=http://192.168.144.1:7890`）
- 单元测试 vs 集成测试边界：单元测一个函数一个行为分支，跨多函数串联是集成测试
- **EIP-8183 Hook 模式**：beforeAction/afterAction 围绕核心函数，claimRefund 不可 Hook
- **SimpleSwapHook 设计**：用 tx.origin 替代 staticcall 回调获取调用者，避免 ABI 解码复杂性
- **Infura Sepolia RPC 不稳定**：`--gas-limit` 可跳过 gas 估计直接广播；`--gas-price` 提高优先打包
- 多个 MockERC20 部署时 bytecode 相同，Etherscan 自动识别为已验证
- ERC-8183 的 Evaluator 只裁决交付质量，不裁决商业条款（价格比较是 Client 的事）

## 存在的问题

- FundTransferHook V1 的 `staticcall` 回调 Escrow.getJob() 返回的 ABI 解码有 bug（address(0)），需后续修复
- CrossReentry.t.sol 缺失（IT-015/IT-016 待实现）
- Infura Sepolia RPC 频繁超时，考虑换 Alchemy 或自建节点

## 明天计划

- 补全 CrossReentry.t.sol（IT-015/IT-016）
- 执行集成测试全量回归（Executor Agent）
- 修复 FundTransferHook staticcall bug，跑通完整两代币流转版
- 准备 Demo 材料

## 投入时间

- 约 8h
